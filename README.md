builpack-safe-assets-compilation
------------------------

## Using

Simply add the buildpack to your Heroku app, e.g.

```
heroku buildpacks:add -a MY-APP https://github.com/trustpair/heroku-buildpack-safe-assets-compilation
```
